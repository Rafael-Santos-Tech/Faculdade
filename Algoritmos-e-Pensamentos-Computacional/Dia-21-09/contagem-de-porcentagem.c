#include <stdio.h>

int main()
{
    int contador = 1;
    
    while (contador <= 100){
        printf("A porcentagem está em: %d% \n", contador);
        contador++;
    }
    return 0;
}
